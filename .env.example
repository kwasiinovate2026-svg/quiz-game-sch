# Example environment variables for production
# If BACKEND_URL is unset, server uses builtin local fallback backend
# Set BACKEND_URL to your backend service URL (https://api.example.com)
BACKEND_URL=
# API_TOKEN is required when BACKEND_URL is set; used for Authorization: Bearer <token>
API_TOKEN=
# Server port
PORT=3000
